A slightly modified version of the default .net 8 wheater API. It creates and fill a database with the random values, if they don't exist.
Otherwise it simply returns those database values.

The project uses EF core and was created as a POC for debugging .net applications with sql server on linux.
